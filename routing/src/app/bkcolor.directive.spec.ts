import { BkcolorDirective } from './bkcolor.directive';

describe('BkcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new BkcolorDirective();
    expect(directive).toBeTruthy();
  });
});
